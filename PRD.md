# PRD：银行流水报表系统 v2

版本：v2.0.1
更新日期：2026-05-11

---

## 1. 背景与目标

玉玄道集团旗下多家子公司分别在不同银行开立账户，每月末由各银行导出对账单（`.xls` / `.xlsx`）。财务部门需将多家银行、多家公司的流水合并，按费用类型与承担部门、收入类型与来源公司进行分类统计，同时展示各账户月度余额，形成全年收支报表，供管理层核对。

本项目为 v1（20260506）的简化重构版，在保留全部功能的基础上，将代码架构从 6 个文件简化为 3 个，并优化了修正补录工作流。

### 1.1 核心目标

1. **自动合并**：一键读取 `input/` 目录下所有银行流水文件，去重、分类、汇总。
2. **支出与收入并重**：单次执行同时输出支出统计报表和收入统计报表，结构对称、风格一致。
3. **账户余额汇总**：按月动态提取各账户期初/期末余额，汇总到余额总览 Sheet，含完整度状态列。
4. **收入智能分类**：内置收入匹配引擎（30+ 条结构化规则），100% 自动匹配，无需人工干预。
5. **支出灵活规则**：精确映射（优先级最高）+ 关键词正则两层规则，规则持续积累。
6. **集团内往来款扣除**：所有汇总表同时展示含往来款合计、往来款金额、扣除后净额。
7. **闭环修正工作流**：未匹配 Sheet 嵌入主报表，财务填写后一键写回规则库并重新生成。
8. **报表增强（内置）**：收支对比 / AutoFilter / 完整度列 / Sparkline，`生成报表.py` 自动应用。
9. **运行隔离**：每次运行在 `output/YYYYMMDD_NNN/` 独立子目录，结果可追溯。

---

## 2. 系统架构

```
20260510_银行流水报表系统/
├── input/                   # 原始银行流水（.xls / .xlsx）
├── output/
│   └── YYYYMMDD_NNN/        # 每次运行独立子目录
├── rules/
│   └── 规则库.xlsx           # 唯一规则文件
└── src/
    ├── _核心库.py            # 内部核心库（勿直接运行）
    ├── 生成报表.py           # 入口一：生成收支报表
    └── 修正补录.py           # 入口二：补充规则，重新生成
```

### 2.1 两程序架构

| 程序 | 文件 | 职责 |
|------|------|------|
| 入口一 | `src/生成报表.py` | 读取流水 → 加载规则 → 生成支出+收入报表 → 嵌入未匹配 Sheet → 输出到子目录 |
| 入口二 | `src/修正补录.py` | 读取已填修正 → 写入规则库 → 触发重新生成 |

`_核心库.py` 分三段（§1 收入匹配引擎 / §2 核心处理库 / §3 报表增强），为内部实现，不对外暴露。

---

## 3. 输入规格

### 3.1 支持的银行格式

| 银行 | 格式 | 识别特征 |
|------|------|---------|
| 招商银行 | `.xlsx` | 表头含"记账日期"、"借方发生额" |
| 光大银行 | `.xls` | 表头含"交易日期"、"支出金额" |
| 农业银行 | `.xls` | 表头含"记账日期"、"借方金额" |
| 平安银行 | `.xls` | 表头含"交易日期"、"支出" |
| 中国银行/致远 | `.xls` | 表头含"交易日期"、"发生额（借/贷）" |
| 鸿明精简 | `.xlsx` | 简化格式，含"日期"、"金额" |

招商银行 `.xlsx` 支持自动提取 `对账单期初余额`、期末流水余额、`对账单余额`。

### 3.2 公司识别

公司名称优先从账户名称字段识别，无法识别时从文件名推断（`infer_bank()` + `infer_company()` 函数）。

---

## 4. 输出规格

### 4.1 输出目录与文件命名

```
output/
  YYYYMMDD_NNN/                                   ← 本次运行独立子目录
    {year}年银行【支出】全年统计_终稿.xlsx          ← 全部匹配时
    {year}年银行【支出】全年统计_待完善.xlsx        ← 含未匹配记录时（内含修正 Sheet）
    {year}年银行【收入】全年统计_终稿.xlsx
    {year}年银行【收入】全年统计_待完善.xlsx
```

- `_终稿`：所有记录均完整匹配，无需修正
- `_待完善`：存在未匹配记录，内含 `★支出/收入未匹配（可补规则）` Sheet 供填写

### 4.2 支出报表 Sheet 结构

| Sheet | 内容 | 单位 |
|---|---|---|
| `各账户余额` | 22账户分公司列示，按月动态提取期初/期末余额，含完整度状态列 | 元 |
| `全年总览` | 统计摘要 + 月度汇总 | 万元 |
| `公司月度汇总` | 行=公司，列=月份，含合计/集团内往来/净支出行 | 万元 |
| `费用类型汇总` | 行=费用类型，列=月份 | 万元 |
| `承担部门汇总` | 行=承担部门，列=月份 | 万元 |
| `M月汇总` | 公司 × 费用类型汇总 | 元 |
| `M月明细` | 全部流水 + AutoFilter + 冻结行 | 元 |
| `M月未匹配`（如有）| 未完整匹配记录 | 元 |
| `★支出未匹配（可补规则）`（_待完善）| 可填写修正的黄色区域 Sheet | — |

### 4.3 收入报表 Sheet 结构

| Sheet | 内容 | 单位 |
|---|---|---|
| `各账户余额` | 同支出报表，含完整度状态列 | 元 |
| `收支综合对比` | 全集团月度净现金流 + 各公司对比，含 AutoFilter | 元 |
| `全年总览（收入）` | 统计摘要 + 月度汇总 | 万元 |
| `公司月度汇总（收入）` | 行=公司，列=月份，末列含 Sparkline 迷你图 | 万元 |
| `收入类型汇总` / `来源公司汇总` | 多维分类汇总 | 万元 |
| `M月收入汇总` | 公司 × 收入类型汇总 | 元 |
| `M月收入明细` | 全部流水，绿色=业务收入，黄色=往来款，含 AutoFilter | 元 |
| `收入匹配汇总` | 按收入类型/公司的笔数、金额、占比 | 元 |
| `★收入未匹配（可补规则）`（_待完善）| 可填写修正的黄色区域 Sheet | — |

### 4.4 各账户余额 Sheet 字段

| 列 | 底色 | 来源 |
|----|------|------|
| A 公司名称 | — | 固定 |
| B 银行账户 | — | 固定 |
| C 来源文件 | — | 自动提取 |
| D M1期初余额 | 绿色（自动）| 当年第1个月期初余额 |
| E M1期末余额 | 绿色（自动）| 第1月最后一笔流水余额 |
| F M2期末余额 | 绿色（自动）| 第2月余额，依此类推 |
| … | 绿色（自动）| 按实际月份数动态扩展 |
| 完整度 | 条件颜色 | ✓完整(绿) / △部分(黄) / ✗全缺失(红) |

> xls 格式账户（光大/农行等）余额列显示"无余额数据"，不影响流水分类统计。

---

## 5. 分类规则

### 5.1 支出规则库（rules/规则库.xlsx）

**支出_精确映射 Sheet**（优先级最高）

| 列名 | 说明 |
|------|------|
| 映射键 | `compact(对方名称)|compact(摘要)|compact(银行名称)` |
| 费用类型 | 标准费用类型名称 |
| 承担部门 | 承担部门名称 |
| 备注 | 可选 |

**支出_关键词规则 Sheet**（精确映射未命中时）

| 列名 | 说明 |
|------|------|
| 优先级 | 数字越小越高 |
| 关键词正则 | Python 正则 |
| 费用类型 | 标准名称 |
| 承担部门 | 标准名称 |

### 5.2 收入匹配引擎（`_核心库.py` §1）

内置智能引擎，30+ 条结构化规则，三层优先级：

| 优先级 | 类型 | 示例 |
|--------|------|------|
| 1（最高）| 全局关键词 | 摘要含"结息/收息" → 利息收入；"电子退库" → 税务退库 |
| 2 | 银行+条件精确匹配 | 传统文化光大 + 上海富友支付 → 伤寒论收款 |
| 3（兜底）| 银行+对方类型 | 医疗集团招行 + 个人 → 还款；is_internal(对方) → 往来款 |

### 5.3 集团内往来款

- 支出：`分子公司借款` / `医馆借款`
- 收入：`借款收入` / `往来款`

---

## 6. 修正补录工作流

```
生成报表.py 运行
  ↓ 若有未匹配记录
  output/YYYYMMDD_NNN/
    {year}年银行【支出】全年统计_待完善.xlsx
      └─ Sheet "★支出未匹配（可补规则）"
         ├─ 只读列（灰/蓝底）：原始流水信息
         └─ 黄色列（可编辑）：修正费用类型 / 修正承担部门 / 核对备注
    {year}年银行【收入】全年统计_待完善.xlsx
      └─ Sheet "★收入未匹配（可补规则）"
         └─ 黄色列：修正收入类型 / 核对备注
  ↓ 财务人员填写黄色列并保存
修正补录.py --input <待完善文件>
  ↓ 读取修正记录 → 写入 rules/规则库.xlsx（精确映射）
  ↓ 触发 生成报表.py 重新运行
  output/YYYYMMDD_NNN+1/
    {year}年银行【支出】全年统计_终稿.xlsx   ← 全部匹配
    {year}年银行【收入】全年统计_终稿.xlsx
```

> 规则自动积累：每次修正后，精确映射 Sheet 新增对应规则，下次运行直接命中。

---

## 7. 报表增强功能

### 7.1 收支综合对比 Sheet

- 自动插入收入报表第 2 位（各账户余额之后）
- 上半：全集团按月 7 行（收入合计 / 往来款收入 / 净收入 / 支出合计 / 往来款支出 / 净支出 / 净现金流）
- 下半：按公司明细，各公司净收入 / 净支出 / 净现金流，含月度拆分 + AutoFilter

### 7.2 AutoFilter + 冻结行

- 所有含"明细"关键词的 Sheet 自动添加 AutoFilter 筛选器和冻结表头

### 7.3 完整度状态列

- 各账户余额 Sheet 末尾自动添加
- ✓ 完整（绿）— 全部余额列均为有效数值
- △ 部分（黄）— 部分余额列"无余额数据"
- ✗ 全缺失（红）— 全部余额列"无余额数据"

### 7.4 Sparkline 迷你折线图

- ZIP 后处理注入 Excel 原生 Sparkline XML，无需第三方图表库
- 在 `公司月度汇总（收入）` 末列添加"趋势"折线图，标记高点/低点

---

## 8. 命令行接口

### 8.1 生成报表.py

```bash
python src/生成报表.py                   # 全年统计（含增强，推荐）
python src/生成报表.py --year 2026       # 指定年份
python src/生成报表.py --expense-only    # 只生成支出
python src/生成报表.py --income-only     # 只生成收入
python src/生成报表.py --no-enhance      # 跳过增强（快速模式）
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--input` | `input/` | 银行流水输入目录 |
| `--output` | `output/` | 报表输出根目录（自动创建子目录）|
| `--year` | 自动识别 | 全年模式年份 |
| `--no-enhance` | — | 跳过报表增强功能 |
| `--expense-only` | — | 只生成支出报表 |
| `--income-only` | — | 只生成收入报表 |

### 8.2 修正补录.py

```bash
python src/修正补录.py                          # 自动找最新 _待完善 报表
python src/修正补录.py --input <文件> [<文件>]  # 手动指定 1-2 份
python src/修正补录.py --no-rerun               # 只更新规则库，不重新生成
python src/修正补录.py --dry-run                # 预览将写入的规则
```

| 参数 | 说明 |
|------|------|
| `--input` | 待完善报表路径，可同时指定支出 + 收入两份 |
| `--output` | output 目录（默认 output/）|
| `--rules` | 规则库路径（默认 rules/规则库.xlsx）|
| `--no-rerun` | 只更新规则库，不触发重新生成 |
| `--dry-run` | 预览模式，不实际写入任何文件 |

---

## 9. 去重规则

```
公司名称 + 交易日期 + 借方金额 + 贷方金额 + 对方名称 + 摘要 + 流水号
```

---

## 10. 报表样式标准

| 用途 | 颜色代码 |
|------|---------|
| 大标题行背景 | `#1F3864`（深海蓝）|
| 表头行背景 | `#2E75B6`（企业蓝）|
| 数据偶数行 | `#EBF3FB`（极浅蓝）|
| 合计/汇总行 | `#2E75B6`（企业蓝）+ 白字 |
| 集团内往来款行 | `#F2F2F2`（浅灰）|
| 净支出/净收入行 | `#E2EFDA`（浅绿）|
| 收入-业务收入行 | `#E2EFDA`（浅绿）|
| 收入-往来款行 | `#FFF2CC`（浅黄）|
| 余额自动提取列 | `#E2EFDA`（浅绿）|
| 修正 Sheet 黄色列 | `#FFF2CC`（浅黄）|
| 完整度-完整 | `#E2EFDA`（绿）|
| 完整度-部分 | `#FFF2CC`（黄）|
| 完整度-全缺失 | `#FFB3B3`（红）|

---

## 11. 验收标准

1. 能读取 `input/` 中混合 `.xls`、`.xlsx` 文件，支持全部已知银行格式。
2. 单次执行生成两份报表，输出到 `output/YYYYMMDD_NNN/` 独立子目录。
3. 全部匹配时文件名含 `_终稿`，有未匹配时含 `_待完善` 并内嵌修正 Sheet。
4. 收入匹配引擎实现 100% 自动分类，0 条未匹配。
5. 支出精确映射规则正确加载（890+ 条），之前配置的规则正常命中。
6. 修正补录.py 可读取 _待完善 报表黄色列，写入规则库并重新生成报表。
7. 修正补录.py 支持同时传入支出 + 收入两份文件。
8. 所有明细 Sheet 含 AutoFilter 和冻结表头。
9. 各账户余额 Sheet 按月动态生成，正确区分三种完整度状态。
10. 公司月度汇总（收入）末列含 Sparkline 迷你折线图。
11. 收支综合对比 Sheet 净现金流数据正确（净收入 − 净支出）。
12. 报表样式符合企业标准，配色与 v1 完全一致。
13. `--no-enhance` 跳过增强功能，生成基础报表。
