# CHANGELOG

> 玉玄道集团 银行流水报表系统 v2 · 版本变更记录

---

## [2.1.0] - 2026-05-11

### 新增功能：图形界面（GUI）

- **`src/界面.py`** — 资金统计软件 全新图形界面入口
  - 深色主题 + 玉玄道金色配色方案，高端大气
  - **双 Tab 布局**：「▶ 生成报表」/「✎ 修正补录」
  - **路径持久化配置**：源文件目录、规则库文件、输出目录三路径自动保存，关闭程序后下次启动无需重新配置
  - **双 Tab 路径同步**：在任意 Tab 修改路径，另一 Tab 自动同步
  - **实时彩色日志**：执行进度逐行显示，成功=绿、错误=红、警告=黄、规则=紫
  - **规则更新弹窗**：修正补录完成后，弹窗详列本次新增/更新的规则明细
  - **操作说明弹窗**：底部「❓ 操作说明」按钮，含完整使用流程说明
  - **状态指示灯**：底部实时显示就绪/执行中/完成/失败状态
  - **防并发保护**：执行期间禁止再次触发，防止文件冲突
  - **打开输出目录**：一键用系统文件管理器打开输出文件夹

- **`assets/`** — 资源目录
  - 存放 `logo.png`（玉玄道 Logo，须手动放置）
  - EXE 打包时自动打包进产物

- **`docs/执行计划.md`** — 项目执行计划文档
  - 记录 GUI 开发、测试、GitHub Actions 打包、长期维护全流程
  - 含 `.github/workflows/build.yml` 完整配置参考
  - 方便后期开发人员快速接手维护

### 软件信息

- 软件名称：**资金统计软件**
- 版权所属：**玉玄道·资金管理部**

### 启动方式

```bash
pip install customtkinter pillow
python src/界面.py
```

---

## [2.0.1] - 2026-05-11

### Bug 修复

- **关键修复：规则库精确映射全部失效**
  - 根本原因：`_核心库.py` 合并时，`§3 报表增强` 的 `_find_header_row(ws, key: str)` 函数与 `§2 核心处理库` 的同名函数 `_find_header_row(ws, required_cols: set)` 产生命名冲突，后者覆盖前者，导致规则库加载时 header 行永远找不到，精确映射读取结果始终为 0 条
  - 修复方式：将 §3 版本重命名为 `_find_header_col`，两个函数各司其职，不再冲突
  - 修复效果：支出精确映射从 0 条恢复至 890 条，之前已配置的规则重新生效

---

## [2.0.0] - 2026-05-10

### 架构重构（基于 v1 [4.1.0] 全量功能）

**v2 保留 v1 全部功能，在此基础上做架构简化：**

#### 源码合并（6 → 3 文件）

| v1 文件 | v2 归并至 |
|---------|----------|
| `收入匹配引擎.py` | `_核心库.py` §1 |
| `核心处理库.py` | `_核心库.py` §2 |
| `报表增强.py` | `_核心库.py` §3 |
| `程序一_生成报表.py` | `生成报表.py` |
| `程序二_支出修正.py` + `程序三_收入修正.py` | `修正补录.py` |

#### 输出结构升级

- 每次运行在 `output/` 下创建独立子目录 `YYYYMMDD_NNN/`，多次运行结果自动隔离
- 文件名含状态后缀：`_待完善`（含未匹配记录）/ `_终稿`（全部匹配）

#### 修正工作流简化

- **废弃**：独立的待修正文件（`*_未匹配记录_待修正.xlsx`）
- **新增**：未匹配修正 Sheet 直接嵌入主报表（`★支出未匹配（可补规则）` / `★收入未匹配（可补规则）`），财务人员在原报表内填写黄色列即可
- **合并**：程序二（支出修正）+ 程序三（收入修正）合并为 `修正补录.py`，可同时处理支出与收入，支持自动检测最新待完善报表

#### 其他改进

- `xlrd` 改为惰性导入，仅在实际读取 `.xls` 文件时才要求安装，`.xlsx` 环境无需 xlrd 亦可运行
- 收入匹配引擎内联至 `_核心库.py`，`_INCOME_MATCHER_AVAILABLE` 始终为 `True`，消除 v1 的 import 不确定性

### 继承自 v1 的全部功能

- 多银行多公司流水自动合并、去重
- 支出精确映射（890+ 条）+ 关键词规则（26 条）两层分类
- 收入智能匹配引擎（30+ 条规则，100% 准确率）
- 各账户余额 Sheet，含按月期初/期末动态提取
- 收支综合对比 Sheet（月度净现金流 + 公司对比）
- AutoFilter + 冻结行（所有明细 Sheet）
- 完整度状态列（各账户余额 Sheet）
- Sparkline 原生迷你折线图（公司月度汇总）
- 集团内往来款扣除逻辑
- 企业级 Excel 样式规范（深海蓝/企业蓝/浅绿/浅黄配色）

---

*v1 历史变更记录详见：`20260506_数据报表_银行数据合并处理/CHANGELOG.md`*
